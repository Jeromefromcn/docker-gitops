apiVersion: v1
kind: ResourceQuota
metadata:
  name: headlamp-quota
  namespace: headlamp
spec:
  hard:
    requests.cpu: 200m
    requests.memory: 256Mi
    limits.cpu: 500m
    limits.memory: 640Mi
