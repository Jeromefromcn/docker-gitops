apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello-backend-baseline
  namespace: pr-lanes
spec:
  parentRefs:
    - group: ""
      kind: Service
      name: hello-backend
  rules:
    - backendRefs:
        - name: hello-backend
          port: 80
