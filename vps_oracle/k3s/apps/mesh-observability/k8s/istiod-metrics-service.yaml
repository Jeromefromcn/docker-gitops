apiVersion: v1
kind: Service
metadata:
  name: istiod-metrics
  namespace: istio-system
spec:
  type: NodePort
  selector:
    app: istiod
    istio: pilot
  ports:
    - port: 15014
      targetPort: 15014
      nodePort: 30110
      name: http-monitoring
