apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
  namespace: lab-environment
  labels:
    app: visits-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      enableServiceLinks: false
      containers:
        - name: visits-service
          image: ops-lab/visits-service:dev
          env:
            - name: TZ
              value: "Asia/Hong_Kong"
            - name: SPRING_CLOUD_CONSUL_HOST
              value: "consul"
            - name: SPRING_CLOUD_CONSUL_PORT
              value: "8500"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_PREFER_IP_ADDRESS
              value: "true"
            # JVM container-detection is broken under this containerd/cgroup v2
            # (MaxHeap defaults to ~6Gi = 25% of host RAM), so the heap grows
            # past the 512Mi limit and gets OOM-killed. The apps only use
            # ~100Mi heap — pin it explicitly.
            - name: JAVA_TOOL_OPTIONS
              value: "-Xmx192m -Xms64m -XX:MaxDirectMemorySize=64m -XX:MaxRAM=512m"
            # RSS sits ~270Mi above what the JVM's own heap/metaspace metrics
            # report (confirmed flat over 15h with zero traffic, so it's not
            # a leak) — glibc's default per-thread malloc arenas over
            # fragment native memory the JVM never sees. Cap arena count
            # instead of leaving it unbounded.
            - name: MALLOC_ARENA_MAX
              value: "2"
          ports:
            - containerPort: 8082
          resources:
            requests:
              cpu: 50m
              memory: 768Mi
            limits:
              cpu: 250m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
  namespace: lab-environment
spec:
  selector:
    app: visits-service
  ports:
    - port: 8082
      targetPort: 8082
