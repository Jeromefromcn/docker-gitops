apiVersion: apps/v1
kind: Deployment
metadata:
  name: trilium
  namespace: workloads
  labels:
    app: trilium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trilium
  template:
    metadata:
      labels:
        app: trilium
    spec:
      # Without this, k8s injects TRILIUM_PORT=tcp://<clusterIP>:8080 (from the
      # `trilium` Service) into the container's env, colliding with trilium's
      # own TRILIUM_PORT config var (which expects a plain integer) and crashing it.
      enableServiceLinks: false
      containers:
        - name: trilium
          image: triliumnext/trilium:v0.104.1
          env:
            - name: TZ
              value: "Asia/Hong_Kong"
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: data
              mountPath: /home/node/trilium-data
          resources:
            requests:
              cpu: 25m
              memory: 320Mi
            limits:
              cpu: 500m
              memory: 640Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: trilium
