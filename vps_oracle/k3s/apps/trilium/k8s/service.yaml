apiVersion: v1
kind: Service
metadata:
  name: trilium
  namespace: workloads
spec:
  type: NodePort
  selector:
    app: trilium
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
