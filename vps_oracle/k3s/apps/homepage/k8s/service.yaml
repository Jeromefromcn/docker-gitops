apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: workloads
spec:
  type: NodePort
  selector:
    app: homepage
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081
