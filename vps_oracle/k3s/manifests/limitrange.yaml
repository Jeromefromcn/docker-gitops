apiVersion: v1
kind: LimitRange
metadata:
  name: workloads-limits
  namespace: workloads
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 200m
        memory: 256Mi
