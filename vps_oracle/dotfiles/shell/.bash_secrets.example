# Copy to .bash_secrets (same directory, blocked from git by .gitignore) and
# fill in real values.
# Sourced by .bashrc via [ -f ~/.bash_secrets ] && . ~/.bash_secrets.
export DEEPSEEK_API_KEY="<deepseek api key, used by the ds-on/ds-off alias pair>"
