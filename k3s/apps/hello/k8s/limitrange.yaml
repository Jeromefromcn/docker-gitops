apiVersion: v1
kind: LimitRange
metadata:
  name: pr-lanes-limits
  namespace: pr-lanes
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 25m
        memory: 64Mi
      default:
        cpu: 100m
        memory: 128Mi
